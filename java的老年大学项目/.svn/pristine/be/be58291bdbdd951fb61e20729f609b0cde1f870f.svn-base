package com.learnyeai.base.api.client;

import com.learnyeai.base.api.vo.CfgSchemeEditionHisVo;
import com.learnyeai.core.support.BaseFeignClient;
import org.springframework.cloud.openfeign.FeignClient;

/**
 * 方案版本历史
 *
 * @author zhangpz
 */
@FeignClient(name = "${BASE-SERVICE-NAME}", path="${BASE-SERVICE-CONTEXT-PATH}/CfgSchemeEditionHis")
public interface CfgSchemeEditionHisClient extends BaseFeignClient<CfgSchemeEditionHisVo> {
}
