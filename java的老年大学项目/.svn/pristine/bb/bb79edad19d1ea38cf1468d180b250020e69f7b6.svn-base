package com.learnyeai.cert.api.client;

import com.learnyeai.cert.api.vo.CtUserCertVo;

import com.learnyeai.core.support.BaseFeignClient;
import org.springframework.cloud.openfeign.FeignClient;

/**
 * 学员证书
 *
 * @author twang
 */
@FeignClient(name = "CERT-INTERFACE", path="cert/CtUserCert")
public interface CtUserCertClient extends BaseFeignClient<CtUserCertVo> {
}
