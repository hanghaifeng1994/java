package com.learnyeai.base.api.client;

import com.learnyeai.base.api.vo.TimeExpiresVo;
import com.learnyeai.core.support.BaseFeignClient;
import org.springframework.cloud.openfeign.FeignClient;

/**
 * 时间到期处理
 *
 * @author zhangpz
 */
@FeignClient(name = "${BASE-SERVICE}", path="/base/TimeExpires")
public interface TimeExpiresClient extends BaseFeignClient<TimeExpiresVo> {
}
